<h{{level}} class="heading -h{{level}}{{#and alternate (or (eq level 1) (eq level 5))}}-alt{{/and}}{{#and hero (eq level 1)}}-hero{{/and}}{{#if classes}} {{classes}}{{/if}}">{{#if icon}}{{> tokens-icon id=icon}} {{/if}}{{heading}}</h{{level}}>
